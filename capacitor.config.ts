import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.foxsgr.github.weborders',
  appName: 'Web Orders',
  webDir: 'dist/apps/webapp',
  bundledWebRuntime: false,
};

export default config;
